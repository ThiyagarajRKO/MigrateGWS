// types/config.ts
export type DomainMapping = Record<string, string[]>;

// DISABLED: Pre-configured domain mappings - manual configuration required
export const domainMappings: DomainMapping = {
  // No pre-configured mappings - users must manually configure all domain mappings
};
